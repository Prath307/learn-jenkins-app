pipeline {
    agent any

    stages {
        stage('Jenkinsfile') {
            steps {
                sh ''' 
                    echo "Hello World from Jenkinsfile"
                    ls
                '''
            }
        }
    }
}